import { test } from '../src/fixtures.js';

test.describe('After the installation ', () => {
  // This could be also done without installation the extension
  test.beforeEach(async () => {
    // Do the onboarding
  });
  test('and login into Ghostery account', () => {
    // 1. Open Ghostery Settings on My Account
    // 2. Click on "Sign In" button
    // 4. https://www.ghostery.com/signin should be opened
    // 5. Sign in
    // 6. Open Ghostery Settings on Me Account and check if the user is signed in
  });
});
