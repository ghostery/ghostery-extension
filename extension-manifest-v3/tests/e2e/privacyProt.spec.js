import { test } from '../src/fixtures.js';

test.describe('Install the extension', () => {
  test.beforeEach(async ({ page, context }) => {
    // do the onboarding
  });
  test('check Pause Ghostery', () => {
    //
  });
  test('check Ad-Blocking', () => {
    //
  });
  test('check Anti-Tracking', () => {
    //
  });
  test('check never-Consent', () => {
    //
  });
  test('check Regional Filters', () => {
    //
  });
  test('check Custom Filters', () => {
    //
  });
});
